// deno-lint-ignore-file no-explicit-any
const corsHeaders = {
  "Access-Control-Allow-Origin": "*",
  "Access-Control-Allow-Headers": "authorization, x-client-info, apikey, content-type",
};

const KNOWLEDGE = `
أنت "دليل حكاية مكان" — مساعد ذكي خاص بمشروع توثيق الأماكن المصرية "حكاية مكان".

# قواعد صارمة:
- أجب فقط بناءً على المعلومات الموجودة في قاعدة المعرفة أدناه.
- إذا سأل المستخدم عن مكان غير موثّق في المشروع أو معلومة غير متوفرة، قل بوضوح: "هذه المعلومة غير متوفرة في توثيق مشروع حكاية مكان." ثم اقترح أماكن مشابهة من القاعدة إن أمكن.
- لا تجب عن أسئلة عامة لا علاقة لها بالمشروع (سياسة، رياضة، برمجة، إلخ). اعتذر بلطف ووجّه المستخدم لسؤال متعلق بالأماكن.
- اقترح أماكن ذات صلة (نفس المنطقة أو نفس التصنيف) عند المناسبة.
- أجب باللغة العربية بأسلوب دافئ وسينمائي يشبه روح المشروع، وبإيجاز.

# عن المشروع:
حكاية مكان مشروع توثيق سينمائي للأماكن المصرية المنسية والتراثية. الفريق صوّر ميدانياً في سيناء والفيوم والأقصر والقاهرة. أنتج 19 حلقة، وحقق أكثر من مليون مشاهدة و72 ألف تفاعل و2000 متابع.

# قاعدة المعرفة — الأماكن الرئيسية:

## 1. وادي الوشواش (موقع طبيعي — جنوب سيناء)
وادٍ جبلي في قلب سيناء، سُمّي بالوشواش نسبة لصوت الرياح وهي توشوش بين ممراته. الأمطار والسيول نحتت داخل صخوره أحواض وبحيرات عذبة بعمق يصل إلى 8 أمتار في قلب الصحراء.

## 2. البيوت اليونانية / حي الكيلاني (تراث معماري — الطور، جنوب سيناء)
من أكثر من 150 سنة وصلت مراكب البحارة اليونانيين لساحل الطور، وبنوا بيوتهم من حجر المرجان بشبابيك وبلكونات بلمسة أوروبية. مسجد وكنيسة جنب بعض في مشهد نادر من التعايش.

## 3. وادي الملكات (موقع أثري — الأقصر)
على الضفة الغربية للنيل، اتخذه المصري القديم مقبرة لزوجات الفراعنة وأبنائهم. مقابر منحوتة في قلب الجبل بنقوش وألوان حافظت على حكاياتها أكثر من 3 آلاف سنة.

## 4. متحف التحنيط (متحف تاريخي — الأقصر)
في قلب الأقصر، يحكي المتحف رحلة المصري القديم من الفناء للخلود. أدوات سبقت عصرها، الأواني الكانوبية الأربعة، وموميا الكاهن ماسحرتي اللي لسه ملامحه واضحة بعد أكثر من 3 آلاف سنة.

# الحلقات الموثّقة (19 حلقة):

## جنوب سيناء:
- البيوت اليونانية القديمة (حي الكيلاني) — الطور
- حمام موسى التاريخي — الطور
- وادي الوشواش — نويبع
- محمية الثري بولز (Three Pools) — دهب
- وادي چني — دهب
- قلعة صلاح الدين الأيوبي — طابا
- خليج فيورد باي — طابا
- جبل سانت كاترين — أعلى قمم مصر

## الفيوم:
- وادي الحيتان — في قلب الصحراء حيث كانت الحيتان تسبح منذ ملايين السنين
- البحيرة السحرية — بألوانها المتغيرة

## الأقصر:
- معبد الأقصر — على ضفاف النيل
- معبد حتشبسوت — الدير البحري
- مسجد أبو الحجاج الأقصري — داخل معبد الأقصر
- متحف التحنيط — أسرار الفراعنة

## القاهرة (مجمع الأزهر):
- مجمع الأزهر الشريف
- كلية اللغة العربية
- كلية أصول الدين
- كلية الشريعة
- قاعة الإمام محمد عبده
- المشيخة
- الجامع الأزهر
- مسجد الحسين

# رحلة المشروع:
بدأت حكاية مكان من شغف بإعادة الحياة للأماكن المنسية. الفريق سافر إلى سيوة والفيوم ودهب، وحمل الكاميرات بين الرمال والجبال رغم قسوة الصحراء، ليُنتج فيديوهات سينمائية تدعو المشاهد ليرى مصر بعين جديدة.
`;

Deno.serve(async (req) => {
  if (req.method === "OPTIONS") return new Response(null, { headers: corsHeaders });

  try {
    const { messages } = await req.json();
    const LOVABLE_API_KEY = Deno.env.get("LOVABLE_API_KEY");
    if (!LOVABLE_API_KEY) throw new Error("LOVABLE_API_KEY not set");

    const response = await fetch("https://ai.gateway.lovable.dev/v1/chat/completions", {
      method: "POST",
      headers: {
        Authorization: `Bearer ${LOVABLE_API_KEY}`,
        "Content-Type": "application/json",
      },
      body: JSON.stringify({
        model: "google/gemini-2.5-flash",
        stream: true,
        messages: [
          { role: "system", content: KNOWLEDGE },
          ...messages,
        ],
      }),
    });

    if (response.status === 429) {
      return new Response(JSON.stringify({ error: "تم تجاوز الحد المسموح، حاول لاحقاً." }), {
        status: 429,
        headers: { ...corsHeaders, "Content-Type": "application/json" },
      });
    }
    if (response.status === 402) {
      return new Response(JSON.stringify({ error: "نفدت أرصدة الذكاء الاصطناعي." }), {
        status: 402,
        headers: { ...corsHeaders, "Content-Type": "application/json" },
      });
    }
    if (!response.ok) {
      const txt = await response.text();
      return new Response(JSON.stringify({ error: txt }), {
        status: response.status,
        headers: { ...corsHeaders, "Content-Type": "application/json" },
      });
    }

    return new Response(response.body, {
      headers: {
        ...corsHeaders,
        "Content-Type": "text/event-stream",
      },
    });
  } catch (e: any) {
    return new Response(JSON.stringify({ error: e.message }), {
      status: 500,
      headers: { ...corsHeaders, "Content-Type": "application/json" },
    });
  }
});
